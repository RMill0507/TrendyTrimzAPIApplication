/*
Deployment script for CustomerDB

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "CustomerDB"
:setvar DefaultFilePrefix "CustomerDB"
:setvar DefaultDataPath "C:\Users\rmill\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"
:setvar DefaultLogPath "C:\Users\rmill\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
/*
The column [dbo].[Customer].[PhoneNumber] on table [dbo].[Customer] must be added, but the column has no default value and does not allow NULL values. If the table contains data, the ALTER script will not work. To avoid this issue you must either: add a default value to the column, mark it as allowing NULL values, or enable the generation of smart-defaults as a deployment option.
*/

IF EXISTS (select top 1 1 from [dbo].[Customer])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
PRINT N'Altering Table [dbo].[Customer]...';


GO
ALTER TABLE [dbo].[Customer]
    ADD [PhoneNumber] INT NOT NULL;


GO
PRINT N'Altering Procedure [dbo].[spCustomer_Get]...';


GO
ALTER PROCEDURE [dbo].[spCustomer_Get]
@Id int
AS
begin
	select Id, FirstName, LastName, ChildsName, ChildsAge, HairCutStyle, PhoneNumber
	from dbo.[Customer]
	where Id = @Id;
end
GO
PRINT N'Altering Procedure [dbo].[spCustomer_GetAll]...';


GO
ALTER PROCEDURE [dbo].[spCustomer_GetAll]
AS
begin
	select Id, FirstName, LastName, ChildsName, ChildsAge, HairCutStyle, PhoneNumber
	from dbo.[Customer];
end
GO
PRINT N'Altering Procedure [dbo].[spCustomer_Insert]...';


GO
ALTER PROCEDURE [dbo].[spCustomer_Insert]
	@FirstName nvarchar(50),
	@LastName nvarchar(50),
	@ChildsName nvarchar(50),
	@ChildsAge int,
	@HairCutStyle nvarchar(max),
	@PhoneNumber nvarchar(50)
 AS 
 begin 
	insert into dbo.[Customer] (FirstName, LastName, ChildsName, ChildsAge, HairCutStyle, PhoneNumber)
	values (@FirstName, @LastName, @ChildsName, @ChildsAge, @HairCutStyle, @PhoneNumber);
end
GO
PRINT N'Altering Procedure [dbo].[spCustomer_Update]...';


GO
ALTER PROCEDURE [dbo].[spCustomer_Update]
	@Id int,
	@FirstName nvarchar(50),
	@LastName nvarchar(50),
	@ChildsName nvarchar(50),
	@ChildsAge int,
	@HairCutStyle nvarchar(max),
	@PhoneNumber nvarchar(50)
AS
begin
	update dbo.[Customer]
	set FirstName = @FirstName, LastName = @LastName, @ChildsName = @ChildsName, @ChildsAge = ChildsAge,
	@HairCutStyle = HairCutStyle, @PhoneNumber = PhoneNumber
	where Id = @Id;
end
GO
PRINT N'Refreshing Procedure [dbo].[spCustomer_Delete]...';


GO
EXECUTE sp_refreshsqlmodule N'[dbo].[spCustomer_Delete]';


GO
if not exists (select 1 from dbo.[Customer])
begin
	insert into dbo.[Customer] (FirstName, LastName, ChildsName, ChildsAge, HairCutStyle, PhoneNumber)
	values ('Robert','Miller', 'Robby', 4, 'fade with scissor cut on top', 4548124845),
	('Grace', 'Potter', 'Vincent', 2, 'Scissor cut trim all around', 2125423542),
	('John', 'Doe', 'James', 3, '1 on sides, mohawk on top', 1111111111),
	('Jane', 'Doe', 'Timmy', 12, 'bald fade', 4875656475);
	end
GO

GO
PRINT N'Update complete.';


GO
